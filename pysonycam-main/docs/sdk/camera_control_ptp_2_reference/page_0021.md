# Page 21

| Name | Code | Description |
| --- | --- | --- |
| GetDeviceInfo | 0x1001 | same as PIMA 15740 |
| OpenSession | 0x1002 | same as PIMA 15740 |
| CloseSession | 0x1003 | same as PIMA 15740 |
| GetObjectInfo | 0x1008 | Response Code includes VendorResponseCodes, Password Length Over Max, and Password Includes Invalid Character. The others are same as PIMA 15740 |
| GetObject | 0x1009 | same as PIMA 15740 |
| GetThumb | 0x100A | same as PIMA 15740 |
| GetPartialObject | 0x101B | same as PIMA 15740 |
| SDIO Connect _ | 0x9201 | This is for the authentication handshake. |
| SDIO GetExtDeviceInfo _ | 0x9202 | Get the protocol version and the supported properties of the connected device. |
| SDIO SetExtDevicePropValue _ | 0x9205 | Set a DevicePropValue for a device property. |
| SDIO ControlDevice _ | 0x9207 | Set the SDIControl value for the SDIControlCode. |
| SDIO GetAllExtDevicePropInfo _ | 0x9209 | Obtain all support DevicePropDescs at one time. The host will send this operation at regular intervals to obtain the latest (current) camera settings. |

| Name | Code | Description |
| --- | --- | --- |
| Compression Setting | 0x5004 | Get/Set the quality. |
| White Balance | 0x5005 | Get/Set the white balance. |
| F-Number | 0x5007 | Get the aperture value. |
| Focus Mode | 0x500A | Get/Set the focus mode. |
| Exposure Metering Mode | 0x500B | Get/Set the exposure metering mode. |
| Flash Mode | 0x500C | Get/Set the flash mode. |
| Exposure Program Mode | 0x500E | Get/Set the exposure program mode. |
| Exposure Bias Compensation | 0x5010 | Get the exposure bias compensation. |
| Still Capture Mode | 0x5013 | Get/Set the drive mode. |

Command List
The following described a list of operation, device property, control, event, etc.
All command IDs not listed in this specification are reserved commands. There may be other commands
executable by the camera that are not listed in this specification, but please refrain from using them.
Operations
Name
Code
Description
GetDeviceInfo
```c
0x1001 same as PIMA 15740
```
OpenSession
```c
0x1002 same as PIMA 15740
```
CloseSession
```c
0x1003 same as PIMA 15740
```
GetObjectInfo
```c
0x1008
```
Response Code includes VendorResponseCodes, Password
Length Over Max, and Password Includes Invalid Character.
The others are same as PIMA 15740
GetObject
```c
0x1009 same as PIMA 15740
```
GetThumb
```c
0x100A same as PIMA 15740
```
GetPartialObject
```c
0x101B same as PIMA 15740
```
SDIO_Connect
```c
0x9201 This is for the authentication handshake.
```
SDIO_GetExtDeviceInfo
```c
0x9202 Get the protocol version and the supported properties of the
```
connected device.
SDIO_SetExtDevicePropValue
```c
0x9205 Set a DevicePropValue for a device property.
```
SDIO_ControlDevice
```c
0x9207 Set the SDIControl value for the SDIControlCode.
SDIO_GetAllExtDevicePropInfo 0x9209
```
Obtain all support DevicePropDescs at one time.
The host will send this operation at regular intervals to obtain the
latest (current) camera settings.
Device Properties
Name
Code
Description
Compression Setting
```c
0x5004 Get/Set the quality.
```
White Balance
```c
0x5005 Get/Set the white balance.
```
F-Number
```c
0x5007 Get the aperture value.
```
Focus Mode
```c
0x500A Get/Set the focus mode.
```
Exposure Metering Mode
```c
0x500B Get/Set the exposure metering mode.
```
Flash Mode
```c
0x500C Get/Set the flash mode.
```
Exposure Program Mode
```c
0x500E Get/Set the exposure program mode.
```
Exposure Bias Compensation
```c
0x5010 Get the exposure bias compensation.
```
Still Capture Mode
```c
0x5013 Get/Set the drive mode.
```
Camera Control PTP 2 Reference
