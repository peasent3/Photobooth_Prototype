# Page 45

| Value | Description |
| --- | --- |
| 0x8053 | Movie Recording (M) |
| 0x8054 | Movie Recording (Auto) |
| 0x8059 | Movie Recording (S&Q Motion (P)) |
| 0x805A | Movie Recording (S&Q Motion (A)) |
| 0x805B | Movie Recording (S&Q Motion (S)) |
| 0x805C | Movie Recording (S&Q Motion (M)) |
| 0x805D | Movie Recording (S&Q Motion (Auto)) |
| 0x8060 | Flash Off |
| 0x8070 | Picture Effect |
| 0x8080 | High Frame Rate (P) |
| 0x8081 | High Frame Rate (A) |
| 0x8082 | High Frame Rate (S) |
| 0x8083 | High Frame Rate (M) |
| 0x8084 | S&Q Motion (P) |
| 0x8085 | S&Q Motion (A) |
| 0x8086 | S&Q Motion (S) |
| 0x8087 | S&Q Motion (M) |
| 0x8088 | Movie |
| 0x8089 | Still |
| 0x808A | F (Movie or S&Q) |
| 0x8090 | Movie F Mode |
| 0x8091 | S&Q F Mode |
| 0x8092 | Interval REC (Movie) F Mode |
| 0x8093 | Interval REC (Movie) (P) |
| 0x8094 | Interval REC (Movie) (A) |
| 0x8095 | Interval REC (Movie) (S) |
| 0x8096 | Interval REC (Movie) (M) |
| 0x8097 | Interval REC (Movie) (Auto) |

Value
Description
```c
0x8053
```
Movie Recording (M)
```c
0x8054
```
Movie Recording (Auto)
```c
0x8059
```
Movie Recording (S&Q Motion (P))
```c
0x805A
```
Movie Recording (S&Q Motion (A))
```c
0x805B
```
Movie Recording (S&Q Motion (S))
```c
0x805C
```
Movie Recording (S&Q Motion (M))
```c
0x805D
```
Movie Recording (S&Q Motion (Auto))
```c
0x8060
```
Flash Off
```c
0x8070
```
Picture Effect
```c
0x8080
```
High Frame Rate (P)
```c
0x8081
```
High Frame Rate (A)
```c
0x8082
```
High Frame Rate (S)
```c
0x8083
```
High Frame Rate (M)
```c
0x8084
```
S&Q Motion (P)
```c
0x8085
```
S&Q Motion (A)
```c
0x8086
```
S&Q Motion (S)
```c
0x8087
```
S&Q Motion (M)
```c
0x8088
```
Movie
```c
0x8089
```
Still
```c
0x808A
```
F (Movie or S&Q)
```c
0x8090
```
Movie F Mode
```c
0x8091
```
S&Q F Mode
```c
0x8092
```
Interval REC (Movie) F Mode
```c
0x8093
```
Interval REC (Movie) (P)
```c
0x8094
```
Interval REC (Movie) (A)
```c
0x8095
```
Interval REC (Movie) (S)
```c
0x8096
```
Interval REC (Movie) (M)
```c
0x8097
```
Interval REC (Movie) (Auto)
Note
After changing the Exposure Program Mode, when setting the shooting parameters, please send the command
after a 500 ms interval. In some models, changing settings such as the shutter speed immediately after
modifying this mode may result in indeterminate values being set.
Exposure Bias Compensation
Summary
Get the exposure bias compensation.
Camera Control PTP 2 Reference
