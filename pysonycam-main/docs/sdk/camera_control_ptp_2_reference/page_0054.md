# Page 54

| Value | Description |
| --- | --- |
| 0x00000000 | BULB |
| 0xFFFFFFFF | nothing to display |
| Other than above values | The real value of shutter speed (Upper two bytes: numerator, Lower two bytes: denominator) In the case of the shutter speed is displayed as "Real Number" on the camera, the denominator is fixed 0x000A. e.g.) 0x000F000A: 0x000F (means 15) / 0x0000A (means 10) = 1.5" In the case of the shutter speed is displayed as "Fraction Number" on the camera, the numerator is fixed 0x0001. e.g.) 0x000103E8: 0x0001 (means 1) / 0x03E8 (means 1000) = 1/1000 |

| Field | Field Order | Size (Bytes) | Datatype | Value |
| --- | --- | --- | --- | --- |
| PropertyCode | 1 | 2 | UINT16 | 0xD20E |
| Datatype | 2 | 2 | UINT16 | 0x0002(UINT8) |
| Get/Set | 3 | 1 | UINT8 | 0x00(Get) |
| IsEnabled | 4 | 1 | UINT8 | variable |
| Reserved | 5 | 1 | UINT8 | 0x00 |
| CurrentValue | 6 | 1 | UINT8 | variable |
| FormFlag | 7 | 1 | UINT8 | 0x02(Enumeration) |

| Value | Description |
| --- | --- |
| 0x01 | Fake Battery |
| 0x02 | Unusable |
| 0x03 | Pre-End Battery |
| 0x04 | Battery Level 1/4 |
| 0x05 | Battery Level 2/4 |
| 0x06 | Battery Level 3/4 |
| 0x07 | Battery Level 4/4 |
| 0x08 | Battery Level 1/3 |
| 0x09 | Battery Level 2/3 |

Value
Value
Description
```c
0x00000000
```

## BULB

```c
0xFFFFFFFF
```
nothing to display
Other than above
values
The real value of shutter speed (Upper two bytes: numerator, Lower two bytes:
denominator)
In the case of the shutter speed is displayed as "Real Number" on the camera, the
```c
denominator is fixed 0x000A.
e.g.) 0x000F000A: 0x000F (means 15) / 0x0000A (means 10) = 1.5"
```
In the case of the shutter speed is displayed as "Fraction Number" on the camera, the
```c
numerator is fixed 0x0001.
e.g.) 0x000103E8: 0x0001 (means 1) / 0x03E8 (means 1000) = 1/1000
```
Note
This deviceProperty is for Get-only. For settings, use ControlCode and refer to Shutter Speed.
Battery Level Indicator
Summary
Get the battery level indicator.
Description
Field
Field Order
Size (Bytes)
Datatype
Value
PropertyCode

## UINT16

```c
0xD20E
```
Datatype

## UINT16

```c
0x0002(UINT8)
```
Get/Set

## UINT8

```c
0x00(Get)
```
IsEnabled

## UINT8

variable
Reserved

## UINT8

```c
0x00
```
CurrentValue

## UINT8

variable
FormFlag

## UINT8

```c
0x02(Enumeration)
```
Value
Value
Description
```c
0x01
```
Fake Battery
```c
0x02
```
Unusable
```c
0x03
```
Pre-End Battery
```c
0x04
```
Battery Level 1/4
```c
0x05
```
Battery Level 2/4
```c
0x06
```
Battery Level 3/4
```c
0x07
```
Battery Level 4/4
```c
0x08
```
Battery Level 1/3
```c
0x09
```
Battery Level 2/3
Camera Control PTP 2 Reference
