# Page 38

After obtaining the properties, the Initiator should check whether the image file exists. If it exists, the
Initiator should obtain it immediately using the PTP standard operation GetObject.
The Initiator can change some property values in the camera by sending a set property command,
SDIO_SetExtDevicePropValue.
To capture the picture, start recording the movie, lock AE or AF, and the Initiator sends Controls to the
camera with SDIO_ControlDevice.
The protocol is described in detail in the following section.
For Connections Using PTP-IP
PTP-IP is available when connected via Wi-Fi Direct (AP Mode), Wi-Fi ST Mode, or Wired LAN. The
designated port number for PTP-IP is TCP 15740 (for SSH connections, please refer to Connection by
SSH). For detailed information regarding the connection interface, please refer to the help guide for each
model. When connecting via PTP-IP, the following settings must be set on the camera:
In ILCE-1, you need to turn on the Remote Shooting setting as follows:
Please set "Network > Cnct./Remote Sht. > Remote Shoot Function > Remote Shooting" to "On".
In pan/tilt type cameras, you need to turn on the SSH setting as follows:
Please set "Network > SSH > SSH Setting" to "On".
For other models, refer to the "PC Remote Function" in the Help Guide. The overall sequence leading up to
a PTP-IP connection proceeds as follows:
Camera Control PTP 3 Reference
