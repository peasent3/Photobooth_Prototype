# Page 50

turn off and on the camera, then you shoot one RAW, those JPEG/HEIF and RAW would have the same
sequence number. Checking the Exif date time is necessary to distinguish the pair.
After shooting a still image, it must get an image like the “Get a File Sequence” above. It is necessary to
obtain the device property Shooting File Info to confirm the number of images before content transfer.
Images exist: GetObjectInfo and GetObject must be executed repeatedly the number of images times. No
image: GetObjectInfo and GetObject need not be executed.
Live View
Live View Sequence:
Responder
Initiator
Responder
Initiator
Please wait until Live View Status becomes Enabled.
alt
```c
[Live View Status (0xD221) is Enabled (0x01)]
[Live View Status (0xD221) is Disabled (0x00)]
```
PTP Remote Protocol Connected
​
SDIO_GetAllExtDevicePropInfo ()
Data (SDIExtDevicePropInfo Dataset Array)
Response (OK)
```c
GetObjectInfo (ObjectHandle=0xFFFFC002)
```
Data (ObjectInfo Dataset)
Response (OK)
```c
GetObject (ObjectHandle=0xFFFFC002)
```
Data (Image File Binary)
Response (OK)
The Initiator can obtain live-view images in the shooting mode. Refer to GetObject for the live view.
Camera Control PTP 3 Reference
