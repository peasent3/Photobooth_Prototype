# Page 56

Please note that not all content referenced by the “uri” attributes in the MediaProfile can be retrieved, as
this depends on models. Even if a “uri” attribute is listed in the MediaProfile, there may be cases where the
corresponding file cannot be retrieved via an HTTP GET request.
Disconnect
The Initiator must execute CloseSession to disconnect. Subsequently, release the PTP connection.
Camera Control PTP 3 Reference
