# Page 334

| Field | Field Order | Size (Bytes) | Datatype | Value |
| --- | --- | --- | --- | --- |
| CurrentValue | 6 | 4 | UINT8 | variable |
| FormFlag | 7 | 1 | UINT8 | 0x02(Enumeration) |

| Value | Description |
| --- | --- |
| 0x01 | S |
| 0x02 | M |
| 0x03 | L |

| Field | Field Order | Size (Bytes) | Datatype | Value |
| --- | --- | --- | --- | --- |
| PropertyCode | 1 | 2 | UINT16 | 0xD138 |
| Datatype | 2 | 2 | UINT16 | 0x0008(UINT64) |
| Get/Set | 3 | 1 | UINT8 | 0x01(Get/Set) |
| IsEnabled | 4 | 1 | UINT8 | variable |
| Reserved | 5 | 4 | UINT32 | 0x0000000000000000 |
| CurrentValue | 6 | 4 | UINT32 | variable |
| FormFlag | 7 | 1 | UINT8 | 0x01(Range) |

| Value | Description |
| --- | --- |
| 0x0000000000000000~0xFFFFFFFFFFFFFFFF | Height and width size [Upper 4bytes], center coordinates [Lower 4bytes] |

Field
Field Order
Size (Bytes)
Datatype
Value
CurrentValue

## UINT8

variable
FormFlag

## UINT8

```c
0x02(Enumeration)
```
Value
Value
Description
```c
0x01
```
S
```c
0x02
```
M
```c
0x03
```
L
AF Free Size and Position Setting
Summary
Get/Set the AF free size and position setting.
Description
Field
Field Order
Size (Bytes)
Datatype
Value
PropertyCode

## UINT16

```c
0xD138
```
Datatype

## UINT16

```c
0x0008(UINT64)
```
Get/Set

## UINT8

```c
0x01(Get/Set)
```
IsEnabled

## UINT8

variable
Reserved

## UINT32

```c
0x0000000000000000
```
CurrentValue

## UINT32

variable
FormFlag

## UINT8

```c
0x01(Range)
```
Value
Value
Description
```c
0x0000000000000000~0xFFFFFFFFFFFFFFFF Height and width size [Upper 4bytes], center
```
coordinates [Lower 4bytes]
Note
The AF Free Size and Position Setting (height, width) [Upper 4 bytes]: the height is set in the upper two
bytes, and the width is set in the lower two bytes.
The AF Free Size and Position Setting (x, y) [Lower 4bytes]: the x coordinate is set in the upper two bytes,
and the y coordinate is set in the lower two bytes.
```c
e.g., AF Free Size (height) is 50, (width) is 50, (x) is 150, (y) is 100, set 0x0032003200960064.
```
Separated min/max/step values for each “height,” “width,” “x,” “y” for AF Free Size and Position range.
Set the range value for each.
For the set of Free Size 1, Free Size 2, and Free Size 3, refer to Focus Area. When each Free Size Setting is
selected, the selected Free Size Setting can be changed.
Camera Control PTP 3 Reference
