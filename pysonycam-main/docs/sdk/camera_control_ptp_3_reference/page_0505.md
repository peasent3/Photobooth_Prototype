# Page 505

| Field | Field Order | Size (Bytes) | Datatype | Value |
| --- | --- | --- | --- | --- |
| PropertyCode | 1 | 2 | UINT16 | 0xD280 |
| Datatype | 2 | 2 | UINT16 | 0x0004(UINT16) |
| Get/Set | 3 | 1 | UINT8 | 0x00(Get) |
| IsEnabled | 4 | 1 | UINT8 | variable |
| Reserved | 5 | 2 | UINT16 | 0x0000 |
| CurrentValue | 6 | 2 | UINT16 | variable |
| FormFlag | 7 | 1 | UINT8 | 0x02(Enumeration) |

| Value | Description |
| --- | --- |
| 0x0000 | No Error |
| 0x0001 | Camera system error |
| 0x0002 | Wi-Fi hardware error |
| 0x0003 | Wired LAN hardware error |
| 0x0004 | AP is not registered |
| 0x0005 | AP is not found(with connection retry) |
| 0x0006 | AP connection error |
| 0x0007 | AP password error |
| 0x0008 | Invalid key or static IP address setting error(WEP) |
| 0x0009 | Invalid key or IP address acquisition error(WEP) |
| 0x000a | IP address acquisition error(DHCP) |
| 0x000b | IP address acquisition error(DNS) |
| 0x000c | Airplane mode is on |
| 0x000d | LAN cable error(with connection retry) |
| 0x000e | FTP server setting is not set |
| 0x000f | FTP server registration setting error(User name or password error) |
| 0x0010 | FTP server is Severed |
| 0x0011 | Certificate verification is not normal |
| 0x0012 | Directory create error |
| 0x0013 | Authority error related to file handling / FTP server is over capacity |
| 0x0014 | Can't recognize USB-LAN conv Adapter |
| 0x0015 | Can't recognize the USB tethering device |
| 0x0016 | Check the device set of connect Device |
| 0x0017 | Failed to connect to server:Reconnecting |
| 0x0018 | Can't transfer to FTP server:Reconnecting |
| 0xFFFF | Unknown errors |

Description
Field
Field Order
Size (Bytes)
Datatype
Value
PropertyCode

## UINT16

```c
0xD280
```
Datatype

## UINT16

```c
0x0004(UINT16)
```
Get/Set

## UINT8

```c
0x00(Get)
```
IsEnabled

## UINT8

variable
Reserved

## UINT16

```c
0x0000
```
CurrentValue

## UINT16

variable
FormFlag

## UINT8

```c
0x02(Enumeration)
```
Value
Value
Description
```c
0x0000
```
No Error
```c
0x0001
```
Camera system error
```c
0x0002
```
Wi-Fi hardware error
```c
0x0003
```
Wired LAN hardware error
```c
0x0004
```
AP is not registered
```c
0x0005
```
AP is not found(with connection retry)
```c
0x0006
```
AP connection error
```c
0x0007
```
AP password error
```c
0x0008
```
Invalid key or static IP address setting error(WEP)
```c
0x0009
```
Invalid key or IP address acquisition error(WEP)
```c
0x000a
```
IP address acquisition error(DHCP)
```c
0x000b
```
IP address acquisition error(DNS)
```c
0x000c
```
Airplane mode is on
```c
0x000d
```
LAN cable error(with connection retry)
```c
0x000e
```
FTP server setting is not set
```c
0x000f
```
FTP server registration setting error(User name or password error)
```c
0x0010
```
FTP server is Severed
```c
0x0011
```
Certificate verification is not normal
```c
0x0012
```
Directory create error
```c
0x0013
```
Authority error related to file handling / FTP server is over capacity
```c
0x0014
```
Can't recognize USB-LAN conv Adapter
```c
0x0015
```
Can't recognize the USB tethering device
```c
0x0016
```
Check the device set of connect Device
```c
0x0017
```
Failed to connect to server:Reconnecting
```c
0x0018
```
Can't transfer to FTP server:Reconnecting
```c
0xFFFF
```
Unknown errors
Camera Control PTP 3 Reference
