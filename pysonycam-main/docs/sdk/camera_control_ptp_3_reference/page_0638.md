# Page 638

| Value | Description |
| --- | --- |
| 0x00000001 | min |
| 0xFFFFFFFF | max |
| 0x00000001 | step |

| Field | Field Order | Size (Bytes) | Datatype | Value |
| --- | --- | --- | --- | --- |
| PropertyCode | 1 | 2 | UINT16 | 0xE109 |
| Datatype | 2 | 2 | UINT16 | 0x0002(UINT8) |
| Get/Set | 3 | 1 | UINT8 | 0x01(Get/Set) |
| IsEnabled | 4 | 1 | UINT8 | Variable |
| Reserved | 5 | 1 | UINT8 | 0x00 |
| CurrentValue | 6 | 1 | UINT8 | Variable |
| FormFlag | 7 | 1 | UINT8 | 0x02(Enumeration) |

| Value | Description |
| --- | --- |
| 0x01 | Graphic Display |
| 0x02 | Display All Info. |
| 0x03 | No Disp. Info. |
| 0x04 | Histogram |
| 0x05 | Level |
| 0x06 | For viewfinder |

Value
Value
Description
```c
0x00000001
```
min
```c
0xFFFFFFFF
```
max
```c
0x00000001
```
step
Note
Use the available DISP mode value obtained by Monitor DISP (Screen Display) Mode Candidates(Still) for
the OR operation.
Multiple values could be set using the OR operation. Therefore, at least one option must be set.
```c
You must select “No Disp. Info. Exposure: On (0x0020)” or “No Disp. Info. Exposure: Timeout (0x0040)”
```
despite the “No Disp. Info.” selection.
```c
If both “No Disp. Info. Exposure: On (0x0020)” and “No Disp. Info. Exposure: Timeout (0x0040)” are set,
“No Disp. Info. Exposure: On (0x0020)” will be preferred.
If neither “No Disp. Info. Exposure: On (0x0020)” nor “No Disp. Info. Exposure: Timeout (0x0040)” are
set, “No Disp. Info. Exposure: On (0x0020)” will be set.
Only when “No Disp. Info. (0x0010)” is selected can “No Disp. Info. Exposure: On (0x0020)” and “No
Disp. Info. Exposure: Timeout (0x0040)” be set.
```
Monitor DISP (Screen Display) Mode(Still)
Summary
Get/Set the Still monitor DISP (screen display) mode.
Description
Field
Field Order
Size (Bytes)
Datatype
Value
PropertyCode

## UINT16

```c
0xE109
```
Datatype

## UINT16

```c
0x0002(UINT8)
```
Get/Set

## UINT8

```c
0x01(Get/Set)
```
IsEnabled

## UINT8

Variable
Reserved

## UINT8

```c
0x00
```
CurrentValue

## UINT8

Variable
FormFlag

## UINT8

```c
0x02(Enumeration)
```
Value
Value
Description
```c
0x01
```
Graphic Display
```c
0x02
```
Display All Info.
```c
0x03
```
No Disp. Info.
```c
0x04
```
Histogram
```c
0x05
```
Level
```c
0x06
```
For viewfinder
Camera Control PTP 3 Reference
