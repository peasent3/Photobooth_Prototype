# Page 716

JobInfoSize: UINT32; // Size of JobInfo (including its own size)
// Version 1.01 or later
// Specify 8
JobID: UINT32; // Job ID
```c
  }[]; // Variable-length
};
```
For Delete
type FTPJobList {
Information: {
Version: UINT16; // Data version (100x value)
reserved: UINT8[2];
```c
  };
```
JobInfoNum: UINT32; // Number of JobInfo
// When specifying all contents or completed contents,
```c
                      // specify 0xFFFF and do not store JobInfo.
```
JobInfo: {
JobInfoSize: UINT32; // Size of JobInfo (including its own size)
// Version 1.01 or later
// Specify 8
JobID: UINT32; // Job ID
```c
  }[]; // Variable-length
};
```
OSDImageMetaInfo
type OSDImageMetaInfo {
Information: {
Version: UINT16; // Data version (100x value)
reserved: UINT8[2];
```c
  };
```
MetaInfo: { // On models that support vertical UI,
// the OSD image is sent rotated to match the camera's orientation.
// The YUV image must be rotated according to
// the “YUV image tilt” before being overlaid.
```c
    YuvLayout: UINT32; // 0x00: none, any other value: exists
```
YuvLayoutInformation: {
X-axisMaximum: UINT32; // Maximum X-axis value of OSD image.
X-axisMaximum: UINT32; // Maximum Y-axis value of OSD image.
YuvPosition: {        // Center reference position.
X-position: UINT32; // X-position of YUV Layout.
Y-position: UINT32; // Y-position of YUV Layout.
Width: UINT32;      // Width of YUV Layout.
Height: UINT32;     // Height of YUV Layout.
```c
      };
```
YUV ImageTilt: UINT32; // YUV image tilt angle 0 degree to 359 degree.
// Version 1.01 or later
```c
    };
  };
};
```
ControlPTZF
type ControlPTZF {
Information: {
Version: UINT16; // Data version (100x value)
reserved: UINT8[2];
```c
  };
```
DriveSetting: { // If Control Type is Absolute/Relative/Direction Drive,
Camera Control PTP 3 Reference
