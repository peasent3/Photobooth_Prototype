# Page 717

// one of the following fields must be stored.
AbsoluteDriveSetting: { // Set when Control Type is Absolute Drive
```c
      PanExists: UINT8; // 0x00: false, any other value: true
```
PanSetting: { // PanSetting only exists when PanExists is true
PanPosition: INT32;
PanSpeed: INT32;
```c
      };
      TiltExists: UINT8; // 0x00: false, any other value: true
```
TiltSetting: { // TiltSetting only exists when TiltExists is true
TiltPosition: INT32;
TiltSpeed: INT32;
```c
      };
    };
```
RelativeDriveSetting: { // Set when Control Type is Relative Drive
```c
      PanExists: UINT8; // 0x00: false, any other value: true
```
PanSetting: { // PanSetting only exists when PanExists is true
PanPosition: INT32;
PanSpeed: INT32;
```c
      };
      TiltExists: UINT8; // 0x00: false, any other value: true
```
TiltSetting: { // TiltSetting only exists when TiltExists is true
TiltPosition: INT32;
TiltSpeed: INT32;
```c
      };
    };
```
DirectionDriveSetting: { // Set when Control Type is Direction Drive
```c
      PanExists: UINT8; // 0x00: false, any other value: true
```
PanSetting: { // PanSetting only exists when PanExists is true
PanSpeed: INT32;
```c
      };
      TiltExists: UINT8; // 0x00: false, any other value: true
```
TiltSetting: { // TiltSetting only exists when TiltExists is true
TiltSpeed: INT32;
```c
      };
    };
  };
};
```
SetPresetPTZF
For Set
type SetPresetPTZF {
Information: {
Version: UINT16; // Data version (100x value)
reserved: UINT8[2];
```c
  };
```
PresetSetSetting: {
PresetNumber: UINT16;
SettingType: UINT8;
```c
    Thumbnail: UINT8; // Set ON (0x01) if you want to create a thumbnail image
```
// from the camera image displayed in live view
```c
                      // Set OFF (0x02) if you do not want to create a thumbnail image
                      // Valid when SettingType is set to “Current Position (0x01)”
  };
};
enum SettingType: UINT8
{
  CurrentPosition  = 0x01
};
```
Camera Control PTP 3 Reference
