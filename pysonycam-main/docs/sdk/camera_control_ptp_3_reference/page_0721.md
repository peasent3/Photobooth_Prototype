# Page 721

EframingCommand
type EframingCommand {
Information: {
Length: UINT32; // Length (bytes) from the next feed to the end.
Version: UINT32; // Eframing Command Version
reserved: UINT8[8];
```c
  };
```
EframingInfo: {
HorizontalDenominator: INT32; // Horizontal position normalization denominator (1024x value).
VerticalDenominator: INT32; // Vertical position normalization denominator (1024x value).
```c
    EframingType: UINT8; // Set the same value as DeviceProperty Eframing Type (0xD122).
```
InputAreaSpecifiedMethod: UINT8; // How to specify the coordinates of the rectangle
// to be set in InputInfo and OutputInfo.
```c
                                     // 0x00: Absolute coordinates
                                     // 0x01: Relative coordinates
```
reserved: UINT8[2];
AreaInfoList: {
AreaNum: UINT32; // Number of Area Info.
reserved: UINT8[4];
AreaInfo: { // Set all AreaInfo required for the specified EFramingType.
// If EFramingType is “None”, “Auto”, “Hold Current Position” or
// “Force Zoom Out”, there is no need to set AreaInfo.
// If EFramingType is “Single” or “PTZ”, set AreaNo to 1.
AreaNo: UINT16; // The unique AreaInfo number required for the specified EframingType.
reserved: UINT8[6];
InputInfo: { // Information on the crop area.
```c
          IsAreaEnable: UINT8; // 0x01: Enable the InputInfo.
                               // 0x00: Disable the InputInfo.
```
reserved: UINT8[7];
Rectangle: {
HorizontalPosition: INT32; // Horizontal position numerator (1024x value).
VerticalPosition: INT32; // Vertical position numerator (1024x value).
Width: INT32; // Width (1024x value).
Height: INT32; // Height (1024x value).
```c
          };
        };
```
reserved: UINT8[24];
```c
      }[]; // Variable-length
    };
  };
};
```
ContentInfoList
type ContentInfoList {
Information: {
Version: UINT16; // Data version (100x value)
reserved: UINT8[2];
```c
  };
```
DatabaseInformation {
DataTime: UINT64; // Unix Time: UTC (ms)
// List creation time
// (no update on addition; deletion or modification requires time update)
PhysicalMediaID: UINT8[64]; // Media ID
DatabaseID: UINT8[64]; // Database ID
SlotID: UINT32; // Slot number
```c
  };
```
ContentsList {
ListNum: UINT32; // Number of Contents
Contents {
Camera Control PTP 3 Reference
