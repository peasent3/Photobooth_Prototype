# Page 729

|  | Data (DevicePropValue:0x02) |  |  |
| --- | --- | --- | --- |
|  | Response (OK) |  |  |
|  | 2. Set “Image ID (String)” (e.g. “Flight A”) SDIO SetExtDevicePropValue (DevicePropCode: 0xD04C) _ |  |  |
|  | Data (DevicePropValue:"Flight A") |  |  |
|  | Response (OK) |  |  |
|  | 3. Set “Image ID (Numerical)” (e.g. 0x00000001) SDIO SetExtDevicePropValue (DevicePropCode: 0xD04B) _ |  |  |
|  | Data (DevicePropValue:0x00000001) |  |  |
|  | Response (OK) |  |  |
|  | 4. Ensure the properties are updated SDIO GetAllExtDevicePropInfo () _ |  |  |
|  | Data (SDIExtDevicePropInfo Dataset Array) |  |  |
|  | Response (OK) |  |  |
|  | he values of al Value) Setting,” (String),” al)” are desired values 5. Take a photo |  |  |
|  | Request single shot using controls (0xD2C1 and 0xD2C2) |  |  |
|  |  |  |  |
|  |  |  |  |
|  | 6. Acquire shooting timing with multi interface shoe/flash sync terminal signal |  |  |
|  | GPS information m the user's GPS module he same ImageID Write “Flight A” an lt [“Still Image Save Destination” contains “Host Device”] If “Still Image Save Destination” is set to “Camera (Memory Card),” this sequence will not occur, and the performance will slightly improve |  |  |
|  | Get the latest image from the camera using GetObjectInfo and GetObject |  |  |
|  |  |  |  |
|  |  |  |  |

Camera
User App / GPS module
1. Set “Image ID (Numerical Value) Setting” to on
2. Set “Image ID (String)” (e.g. “Flight A”)
```c
3. Set “Image ID (Numerical)” (e.g. 0x00000001)
```
4. Ensure the properties are updated
5. Take a photo
6. Acquire shooting timing with multi
```c
interface shoe/flash sync terminal signal
```
If “Still Image Save Destination” is
set to “Camera (Memory Card),”
this sequence will not occur,
and the performance will slightly improve
When the JPEG file transferred to the host PC
alt
[“Still Image Save Destination” contains “Host Device”]
```c
SDIO_SetExtDevicePropValue (DevicePropCode: 0xD092)
Data (DevicePropValue:0x02)
```
Response (OK)
```c
SDIO_SetExtDevicePropValue (DevicePropCode: 0xD04C)
```
Data (DevicePropValue:"Flight A")
Response (OK)
```c
SDIO_SetExtDevicePropValue (DevicePropCode: 0xD04B)
Data (DevicePropValue:0x00000001)
```
Response (OK)
SDIO_GetAllExtDevicePropInfo ()
Data (SDIExtDevicePropInfo Dataset Array)
Response (OK)
Check that the values of
“Image ID (Numerical Value) Setting,”
“Image ID (String),”
and “Image ID (Numerical)” are desired values
```c
Request single shot using controls (0xD2C1 and 0xD2C2)
```
Obtain and save GPS information
(location, time, etc.) from the user's GPS module
in the host with the same ImageID
```c
Write “Flight A” and 0x00000001 in Exif
```
Get the latest image from the camera using GetObjectInfo and GetObject
Camera Control PTP 3 Reference
