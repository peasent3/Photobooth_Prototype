libusb1>=3.0.0

# Optional GUI / computer-vision dependencies are NOT required by the library.
# Install them only if you want to run the OpenCV-based examples:
#   pip install -r examples/requirements.txt
